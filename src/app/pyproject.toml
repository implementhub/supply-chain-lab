[project]
name = "app"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.0",
    "requests>=2.32.0",
    "urllib3>=2.6.0",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
]
